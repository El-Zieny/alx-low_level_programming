#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * rev_string - exactly
 * @s: the strin
 */
void rev_string(char *s)
{
	char *c = strrev(s);

	
	
	
	
	
	
	
	
	
	
	/**	char *c;
	int i, n = strlen(s), m = n;

	for (i = 0; i < n; i++)
		*(c + i) = s[i];
	for (i = 0; i < n; i++)
	{
		*(s + i) = c[m];
		m--;
	}
*/













































/**	int n = sizeof(s) - 1;
	char c[n];
	int i, l;
	int x;

	l = 0;
	i = strlen(s) - 1;
	for (x = 0; x <= i; x++)
		c[x] = s[x];
	for (; i >= 0; i--)
	{
		s[i] = c[l];
		l += 1;
	}
	*/
}

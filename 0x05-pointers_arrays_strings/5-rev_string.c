#include "main.h"
#include <string.h>
/**
 * rev_string - exactly
 * @s: the strin
 */
void rev_string(char *s)
{
	const int i = strlen(s) - 1;
	char c[i];










































/**	int n = sizeof(s) - 1;
	char c[n];
	int i, l;
	int x;

	l = 0;
	i = strlen(s) - 1;
	for (x = 0; x <= i; x++)
		c[x] = s[x];
	for (; i >= 0; i--)
	{
		s[i] = c[l];
		l += 1;
	}
	*/
}
